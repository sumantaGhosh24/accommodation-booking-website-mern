import useTitle from "../../hooks/useTitle";

const Dashboard = () => {
  useTitle(`Dashboard`);

  return (
    // show total number of everything
    <div>
      <h1>dashboard</h1>
    </div>
  );
};

export default Dashboard;
